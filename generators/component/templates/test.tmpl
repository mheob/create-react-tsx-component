import { render } from '@testing-library/react'
import { {{.Name}}, type Props } from './{{.FileName}}'

const mockedArgs: Props = {}

const getComponent = (props: Partial<{{.Name}}Props> = mockedArgs) => <{{.Name}} {...mockedArgs} {...props} />

describe('{{.Name}}', () => {
  it('should render correctly', () => {
  	render(getComponent())
  })
})
